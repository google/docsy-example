# API Configuration
GITHUB_DATA_URL=https://your-api-endpoint.com/users

# User Configuration - Comma-separated list of usernames to fetch
# If not provided, defaults to: baruchiro,UrielOfir
USERNAMES=baruchiro,UrielOfir,username3

# Output Configuration
# Directory where the output file will be saved (relative to script location)
OUTPUT_DIR=data
# Name of the output JSON file

OUTPUT_FILE=github_data.json

# Request Configuration
# Request timeout in milliseconds (default: 15000 = 15 seconds)
REQUEST_TIMEOUT=15000

# Maximum number of retry attempts for failed requests (default: 3)
MAX_RETRIES=3

# Delay between retry attempts in milliseconds (default: 2000 = 2 seconds)
RETRY_DELAY=2000

# Logging Configuration
# Log level: debug, info, warn, error (default: info)
# debug - Shows all debug information
# info - Shows normal operation info
# warn - Shows warnings only
# error - Shows errors only
LOG_LEVEL=info

# Example configurations:
# For verbose debugging:
# LOG_LEVEL=debug
# REQUEST_TIMEOUT=30000

# For production (quiet):
# LOG_LEVEL=warn
